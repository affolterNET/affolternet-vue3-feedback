// Common types for the library
export type AnIcon = { class?: string, content?: string }

// Re-export toast types for consistency
export type { ToastType, ToastParams } from './services/ToastService';